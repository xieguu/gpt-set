{
  "name": "gpt-set-client",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated multi-account GPT browser client",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check src/main.js && node --check src/mcp-manager.js && node --check src/preload.js && node --check src/renderer.js",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "electron": "^37.2.6"
  }
}
