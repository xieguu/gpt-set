{
  "name": "gpt-set-local-files-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authenticated MCP server for fixed-folder text and image CRUD",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --check src/server.js",
    "healthcheck": "node src/healthcheck.js",
    "tunnel": "cloudflared tunnel --url http://127.0.0.1:8787"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "zod": "^3.24.2"
  }
}
