// GPT Set may replace this file in an environment-specific extension copy.
// Values saved through the popup remain higher-priority local overrides.
export default {
  endpoint: 'http://127.0.0.1:8787/bridge/capture',
  token: '',
  enabled: true,
  managed: false,
};
